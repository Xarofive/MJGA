import org.junit.FixMethodOrder;
import org.junit.Test;
import org.junit.runners.MethodSorters;

@FixMethodOrder(MethodSorters.NAME_ASCENDING)
public class Task10_4 {
    @Test
    public void test1_returnsGrades() {
        check(100, "A");
        check(90, "A");
        check(89, "B");
        check(70, "C");
        check(60, "D");
        check(0, "F");
    }

    @Test
    public void test2_handlesInvalidScores() {
        check(101, "invalid");
        check(-1, "invalid");
    }

    private void check(int score, String expected) {
        String actual = grade(score);
        if (!expected.equals(actual)) {
            failWithoutStackTrace("Для балла " + score + " ожидалось \"" + expected +
                    "\", но метод вернул \"" + actual + "\".");
        }
    }

    private void failWithoutStackTrace(String message) {
        AssertionError error = new AssertionError(message);
        error.setStackTrace(new StackTraceElement[0]);
        throw error;
    }

    //START_STUDENT_PLACEHOLDER//
    // Твой код
    //END_STUDENT_PLACEHOLDER//
}

// ожидаемый код решения

public static String grade(int score) {
    if (score < 0 || score > 100) {
        return "invalid";
    }

    return switch (score / 10) {
        case 10, 9 -> "A";
        case 8 -> "B";
        case 7 -> "C";
        case 6 -> "D";
        default -> "F";
    };
}

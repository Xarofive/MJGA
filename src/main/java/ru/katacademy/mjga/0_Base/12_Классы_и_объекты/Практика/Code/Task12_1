import org.junit.FixMethodOrder;
import org.junit.Test;
import org.junit.runners.MethodSorters;

@FixMethodOrder(MethodSorters.NAME_ASCENDING)
public class Task12_1 {
    @Test
    public void test1_returnsStudentCard() {
        Student student = new Student();
        student.name = "Alice";
        student.age = 20;
        student.averageGrade = 4.7;

        check("Student: Alice, age: 20, grade: 4.7", student.getCard());
    }

    @Test
    public void test2_usesObjectFields() {
        Student student = new Student();
        student.name = "Bob";
        student.age = 18;
        student.averageGrade = 5.0;

        check("Student: Bob, age: 18, grade: 5.0", student.getCard());
    }

    private void check(String expected, String actual) {
        if (!expected.equals(actual)) {
            failWithoutStackTrace("Ожидалась строка \"" + expected + "\", но получена \"" + actual + "\".");
        }
    }

    private void failWithoutStackTrace(String message) {
        AssertionError error = new AssertionError(message);
        error.setStackTrace(new StackTraceElement[0]);
        throw error;
    }
}

//START_STUDENT_PLACEHOLDER//
// Твой код
//END_STUDENT_PLACEHOLDER//

// ожидаемый код решения

class Student {
    String name;
    int age;
    double averageGrade;

    String getCard() {
        return "Student: " + name + ", age: " + age + ", grade: " + averageGrade;
    }
}
